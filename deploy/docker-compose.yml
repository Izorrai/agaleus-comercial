services:
  comercial-web:
    image: nginx:alpine
    container_name: comercial-web
    restart: unless-stopped
    ports:
      - "5051:80"
    volumes:
      - ../:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
